# Quickstart Workspace

This workspace is intentionally risky.

It exists so you can run `npm run quickstart` and see real MCP Preflight findings immediately, without pointing the scanner at your own repo first.

The config uses fake values and a fake server name on purpose. Nothing here is meant to be used as a real MCP setup.
