# Example Findings Workspace

This workspace exists to show what MCP Preflight findings look like on a small self-contained example.

Run `npm run quickstart` to scan it from the repo root.

What it includes:
- placeholder credentials inside a remote MCP URL
- broad local scope
- environment passthrough
- an ephemeral launcher

Use it as a scan example, not as a template for a real MCP setup.
